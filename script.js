// script.js\n\n// Function to handle search functionality\nfunction handleSearch() {\n    const searchInput = document.getElementById('searchInput').value;\n    if (searchInput) {\n        fetchData(searchInput);\n    }\n}\n\n// Function to fetch data from breach API\nasync function fetchData(query) {\n    try {\n        const response = await fetch(`https://example-breach-api.com/search?query=${query}`);\n        const data = await response.json();\n        updateContent(data);\n    } catch (error) {\n        console.error('Error fetching data:', error);\n    }\n}\n\n// Function to update the dynamic content on the page\nfunction updateContent(data) {\n    const resultsContainer = document.getElementById('results');\n    resultsContainer.innerHTML = ''; // Clear previous results\n\n    if (data.length) {\n        data.forEach(entry => {\n            const resultItem = document.createElement('div');\n            resultItem.className = 'result-item';\n            resultItem.innerText = `Breach: ${entry.breach}, Date: ${entry.date}`;\n            resultsContainer.appendChild(resultItem);\n        });\n    } else {\n        resultsContainer.innerText = 'No results found.';\n    }\n}\n\n// Event listener for search button\ndocument.getElementById('searchButton').addEventListener('click', handleSearch);\n